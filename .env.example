DATABASE_URL="file:./dev.db"
# Resolved relative to prisma/ (where schema.prisma lives), so this
# points at prisma/dev.db — not a path relative to the project root.
AUTH_SECRET="replace-with-a-long-random-string"
